merge_protections:
  - name: RECENT_PUSH_CONTROL
    if: true
    success_conditions: true

merge_protections_settings:
  auto_merge_conditions:
    - label = vm1-recent-push-impact

pull_request_rules:
  - name: BASE_CONFIG_RULE_61A4
    conditions:
      - base = main
    actions:
      comment:
        message: BASE_CONFIG_MARKER_61A4
