pull_request_rules:
  - name: BASE_CONFIG_RULE_61A4
    conditions:
      - base = main
    actions:
      comment:
        message: BASE_CONFIG_MARKER_61A4

  - name: VM1_CODEOWNERS_EMAIL_OWNER_PARITY
    conditions:
      - title = VM1_CODEOWNERS_EMAIL_OWNER_PARITY
      - label = vm1-codeowners-email-trigger
    actions:
      merge:
        method: squash
