merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
merge_protections:
  - name: VM1 large-config security gate
    if:
      - base = main
    success_conditions:
      - label = vm1-large-config-security-approved
